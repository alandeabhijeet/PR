```cpp
// sort.hpp
#ifndef SORT_HPP
#define SORT_HPP
#include <vector>

/**
 * @file sort.hpp
 * @brief This file provides a function for sorting vectors of integers using the Bubble Sort algorithm.
 */

/**
 * Sorts a vector of integers in ascending order using the Bubble Sort algorithm.
 *
 * @param data The vector of integers to be sorted.  The vector is sorted in-place.
 */
void bubble_sort(std::vector<int>& data);

#endif // SORT_HPP
```